@using WebApplicationTestMVC.Models;
@model IEnumerable<Product>
@{
    ViewBag.Title = "Index";
}
@foreach(var p in Model)
{
    <div>
        <h3>@p.name</h3>
        <h4>@p.productinfo</h4>
    </div>
}

<h2>Test success</h2>

